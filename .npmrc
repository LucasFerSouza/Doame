# Hoist all packages to root node_modules to avoid duplication
shamefully-hoist=true

# Strict peer dependencies
strict-peer-dependencies=false

# Auto-install peer dependencies
auto-install-peers=true
